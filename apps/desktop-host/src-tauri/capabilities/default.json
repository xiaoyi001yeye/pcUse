{
  "identifier": "default",
  "description": "Default capability for PC-Use Agent main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:default",
    "shell:allow-open"
  ]
}
